# Copy to .env and update values
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=your_db_name

# Optional older names used in scripts
# user=your_db_user
# password=your_db_password
# host=127.0.0.1
# port=5432
# dbname=your_db_name

# Supabase (if you prefer using Supabase, set these instead of direct DB vars)
# SUPABASE_URL=https://xyzcompany.supabase.co
# SUPABASE_KEY=your-service-role-or-service-key

# If you use Supabase, create the table in SQL editor with:
# create table if not exists clients (
#   id serial primary key,
#   name text not null,
#   address text,
#   phone text,
#   created_at timestamptz default now()
# );